server:
  port: 9090
  # spring boot rest exception message empty
  error:
    include-message: always
    include-binding-errors: on-param

spring:
  boot:
    admin:
      discovery:
        converter:
          management-context-path: /actuator
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
  jpa:
    show-sql: true
    #generate-ddl: true
    #hibernate:
      #ddl-auto: create
    # Permet de generer automatique la creation des tables a partir des entit�s
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  #datasource:
    #initialization-mode: always
############# Mail Properties ###########################################
# smtp.gmail.com ou smtp-relay.gmail.com
  mail:
    host: smtp.gmail.com
    port: 465
    username: serge.kambou@gmail.com
    password: M4th1s1213
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: true
    test-connection: false

#Exposition des endpoints avec actuator
#management.endpoints.web.exposure.include=*
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,beans,env

#datasource
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.url=jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
#spring.datasource.username=sa
#spring.datasource.password=sa

#Info
info:
  app:
    version: 1.0-Beta

#App Properties
evoucherlunch:
  jwt:
    secret: eVoucherLunchSecretKey
    expiration-time: 86400000
